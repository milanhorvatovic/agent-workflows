# workflows/

Composable stages in `stages/` plus the workflows that reference them — `feature.md`, `bugfix.md`, `plan.md`. Workflows compose stages; they never restate stage bodies. Not yet populated.
