# INVALID on purpose — schema validation MUST reject this fixture:
# - imports[0].artifact carries a `..` segment: an import's path is a copy
#   instruction, so it must be the artifact's normalized {run}-relative path —
#   anywhere else would let the copy read or write outside the two run
#   directories the record names (spec §8.6)
run:
  id: 2026-08-16-feature-slug
  workflow: feature
  protocol: "0.2"
steps:
  - id: brief-confirm
    status: skipped
gates: []
artifacts:
  - "{run}/request.md"
  - "{run}/../2026-08-12-plan-slug/brief.md"
imports:
  - artifact: "{run}/../2026-08-12-plan-slug/brief.md"
    from: 2026-08-12-plan-slug
    at: "2026-08-16T09:00:00Z"
