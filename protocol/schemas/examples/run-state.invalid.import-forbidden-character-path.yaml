# INVALID on purpose — schema validation MUST reject this fixture:
# - imports[0].artifact carries `?`, one of the Windows-forbidden
#   filename characters (? * " < > |): the executor cannot perform the
#   recorded copy on Windows, so the record is a defect at validation
#   rather than a failure at copy time (spec §8.6)
run:
  id: 2026-08-16-feature-slug
  workflow: feature
  protocol: "0.2"
steps:
  - id: brief-confirm
    status: skipped
gates: []
artifacts:
  - "{run}/request.md"
  - "{run}/plan?.md"
imports:
  - artifact: "{run}/plan?.md"
    from: 2026-08-12-plan-slug
    at: "2026-08-16T09:00:00Z"
