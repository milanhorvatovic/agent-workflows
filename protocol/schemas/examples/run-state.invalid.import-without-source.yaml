# INVALID on purpose — schema validation MUST reject this fixture:
# - imports[0] carries no `from`: an import without its source run is lineage
#   that names no lineage (spec §8.6)
run:
  id: 2026-08-16-feature-slug
  workflow: feature
  protocol: "0.2"
steps:
  - id: brief-confirm
    status: skipped
gates: []
artifacts:
  - "{run}/request.md"
  - "{run}/brief.md"
imports:
  - artifact: "{run}/brief.md"
    at: "2026-08-16T09:00:00Z"
