# INVALID on purpose — schema validation MUST reject this fixture:
# - imports[0].artifact has a segment ending in a space: Windows
#   normalization strips it, so the recorded string and the copied file
#   silently diverge (spec §8.6)
run:
  id: 2026-08-16-feature-slug
  workflow: feature
  protocol: "0.3"
steps:
  - id: brief-confirm
    status: skipped
gates: []
artifacts:
  - "{run}/request.md"
  - "{run}/brief.md "
imports:
  - artifact: "{run}/brief.md "
    from: 2026-08-12-plan-slug
    at: "2026-08-16T09:00:00Z"
