# INVALID on purpose — schema validation MUST reject this fixture:
# - run.risk_rationale is present without run.risk, the other half of the
#   pairing (spec §5.3, §10) — a rationale for a class run state does not hold
run:
  id: 2026-08-10-half-written
  workflow: feature
  risk_rationale: "single module, no security surface"
  protocol: "0.3"
steps: []
gates: []
artifacts:
  - "{run}/request.md"
