# INVALID on purpose — schema validation MUST reject this fixture:
# - run.id carries U+2029, the Unicode paragraph separator: a directory
#   name that Unicode-aware line tooling renders as two records is no
#   plain directory name, C1 protections notwithstanding (spec §8.1, §8.6)
run:
  id: "2026-08-16\u2029feature"
  workflow: feature
  protocol: "0.3"
steps:
  - id: brief-confirm
    status: pending
gates: []
artifacts:
  - "{run}/request.md"
