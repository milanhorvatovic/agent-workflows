# INVALID on purpose — schema validation MUST reject this fixture:
# - run.id is a DOS device basename: `COM1` under the runs directory
#   resolves to the device namespace on Windows, so it can name no run
#   directory (spec §8.1, §8.6)
run:
  id: COM1
  workflow: feature
  protocol: "0.2"
steps:
  - id: brief-confirm
    status: pending
gates: []
artifacts: []
