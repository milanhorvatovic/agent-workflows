# INVALID on purpose — schema validation MUST reject this fixture:
# a single trigger declares both `every` and `cron`; each is valid only for
# its own kind and a trigger must never carry both (spec §9.3).
protocol: "0.2"
trigger:
  kind: interval
  every: 5m
  cron: "0 9 * * 1"
