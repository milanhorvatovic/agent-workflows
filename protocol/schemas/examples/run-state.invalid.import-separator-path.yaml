# INVALID on purpose — schema validation MUST reject this fixture:
# - imports[0].artifact carries U+2028, the Unicode line separator:
#   outside C0, C1, and DEL, yet Unicode-aware line tooling still splits
#   on it, rendering one copy instruction as several records (spec §8.6)
run:
  id: 2026-08-16-feature-slug
  workflow: feature
  protocol: "0.3"
steps:
  - id: brief-confirm
    status: skipped
gates: []
artifacts:
  - "{run}/request.md"
  - "{run}/brief\u2028.md"
imports:
  - artifact: "{run}/brief\u2028.md"
    from: 2026-08-12-plan-slug
    at: "2026-08-16T09:00:00Z"
