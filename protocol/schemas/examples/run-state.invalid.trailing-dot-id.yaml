# INVALID on purpose — schema validation MUST reject this fixture:
# - run.id ends in a dot: Windows normalization strips trailing dots and
#   spaces, so `demo.` and `demo` resolve to one directory while comparing
#   as two strings — an alias the self-import comparison must not have to
#   see through (spec §8.1, §8.6)
run:
  id: "2026-08-16-feature-slug."
  workflow: feature
  protocol: "0.2"
steps:
  - id: brief-confirm
    status: pending
gates: []
artifacts:
  - "{run}/request.md"
