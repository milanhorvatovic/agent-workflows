# INVALID on purpose — schema validation MUST reject this fixture:
# - imports[0].artifact ends in a newline: `$` alone would accept it — a
#   regex `$` may match just before a final line terminator — so the pattern
#   anchors to the true end of input; a multiline copy destination is the
#   control-character defect in its most escapable form (spec §8.6)
run:
  id: 2026-08-16-feature-slug
  workflow: feature
  protocol: "0.2"
steps:
  - id: brief-confirm
    status: skipped
gates: []
artifacts:
  - "{run}/request.md"
  - "{run}/brief.md\n"
imports:
  - artifact: "{run}/brief.md\n"
    from: 2026-08-12-plan-slug
    at: "2026-08-16T09:00:00Z"
