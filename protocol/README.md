# protocol/

The versioned protocol surface: [`spec.md`](spec.md) (roles, execution modes, risk classes, gates, artifact conventions, orchestration metadata, versioning policy) and [`schemas/`](schemas/) (the normative JSON Schemas, with commented starter fixtures in `schemas/examples/`).
