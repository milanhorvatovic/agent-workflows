# Dependencies for scripts/validate_conformance.py — jsonschema (draft
# 2020-12, with rfc3339-validator so `format: date-time` is asserted, not
# annotation-only) and ruamel.yaml (YAML 1.2), pinned exactly with their
# transitive closure so CI installs are reproducible.
attrs==26.1.0
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
referencing==0.37.0
rfc3339-validator==0.1.4
rpds-py==2026.6.3
ruamel.yaml==0.19.1
six==1.17.0
