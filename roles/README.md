# roles/

Six role files — `analyst`, `planner`, `implementer`, `reviewer`, `validator`, `arbiter` — each ≤ ~50 lines. Roles are protocol semantics (WHO), never session requirements. Not yet populated.
