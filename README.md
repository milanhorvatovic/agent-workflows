# agent-workflows

Tool-agnostic prompt framework for structured, human-gated AI development. Three-tier model — roles (WHO), skills (WHAT), workflows (HOW) — with markdown artifacts carrying state between isolated sessions. A protocol, not a platform: no SDK, no runtime, no lock-in. Works with Claude Code, Codex, Cursor, and any agent that can read a prompt.
