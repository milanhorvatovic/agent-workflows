# driver/

Python reference driver (stdlib only): state machine, context assembly, pluggable invocation backends, loop-contract evaluation, gate handling. The guaranteed execution floor — a human or a capable harness can substitute at any step. Not yet populated.
